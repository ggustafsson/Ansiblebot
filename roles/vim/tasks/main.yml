- name: Git clone/pull {{ git_url_staticcolorscheme }}
  ansible.builtin.git:
    repo: "{{ git_url_staticcolorscheme }}"
    dest: "{{ path_home }}/Projects/Static-Color-Scheme"

- name: Create directories under {{ path_vim }}
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: 0700
  loop:
    - "{{ path_vim }}"
    - "{{ path_vim }}/backup"
    - "{{ path_vim }}/swap"
    - "{{ path_vim }}/undo"
    - "{{ path_vim }}/pack/ggustafsson/start"
    - "{{ path_vim }}/pack/others/start"

- name: Create symbolic links to Dot-Files and Vim colorscheme
  ansible.builtin.file:
    src: "{{ path_projects }}/{{ item.src }}"
    dest: "{{ item.dest }}"
    state: link
  loop:
    - src: Dot-Files/vimrc
      dest: "{{ path_home }}/.vimrc"
    - src: Dot-Files/vim/after
      dest: "{{ path_vim }}/after"
    - src: Dot-Files/vim/templates
      dest: "{{ path_vim }}/templates"
    - src: Static-Color-Scheme
      dest: "{{ path_vim }}/pack/ggustafsson/start/Static-Color-Scheme"

- name: Git clone/pull misc Vim plugins
  ansible.builtin.git:
    repo: "{{ item.url }}"
    dest: "{{ path_vim }}/pack/others/start/{{ item.name }}"
  loop:
    - name: ansible-vim
      url: https://github.com/pearofducks/ansible-vim.git
    - name: syntastic
      url: https://github.com/vim-syntastic/syntastic.git
    - name: vim-go
      url: https://github.com/fatih/vim-go.git
    - name: vim-mundo
      url: https://github.com/simnalamburt/vim-mundo.git
    - name: fern.vim
      url: https://github.com/lambdalisue/fern.vim.git
    - name: fern-hijack.vim
      url: https://github.com/lambdalisue/fern-hijack.vim.git
    - name: fern-git-status.vim
      url: https://github.com/lambdalisue/fern-git-status.vim.git
    - name: vim-commentary
      url: https://github.com/tpope/vim-commentary.git
    - name: vim-endwise
      url: https://github.com/tpope/vim-endwise.git
    - name: vim-repeat
      url: https://github.com/tpope/vim-repeat.git
    - name: vim-speeddating
      url: https://github.com/tpope/vim-speeddating.git
    - name: vim-surround
      url: https://github.com/tpope/vim-surround.git
