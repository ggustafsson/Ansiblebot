- name: Install misc packages
  ansible.builtin.apt:
    pkg:
      - ansible-lint
      - flake8
      - pylint3
      - shellcheck
      - tidy
      - yamllint
    state: latest
  become: yes
  register: apt

- name: "Install misc packages : debug"
  ansible.builtin.debug:
    var: apt.stdout
