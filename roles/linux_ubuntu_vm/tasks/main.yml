- name: Install misc packages
  ansible.builtin.apt:
    pkg:
      - ansible-lint
      - flake8
      - pylint3
      - shellcheck
      - tidy
      - yamllint
    state: latest
  become: yes
  register: apt

- debug: var=apt.stdout
